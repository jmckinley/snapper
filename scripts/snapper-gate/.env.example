# Snapper Gateway Listener — Environment Variables
# Copy this file to .env and fill in the required values.

# ── Required ──────────────────────────────────────────
OPENCLAW_GATEWAY_TOKEN=           # Copy from OpenClaw's .env (grep OPENCLAW_GATEWAY_TOKEN /opt/openclaw/.env)
SNAPPER_URL=http://127.0.0.1:8000
SNAPPER_AGENT_ID=openclaw-main
SNAPPER_API_KEY=                  # From Snapper agent registration (snp_xxx)

# ── Optional ──────────────────────────────────────────
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789   # Gateway WebSocket URL
APPROVAL_TIMEOUT=300                         # Seconds to wait for approval
